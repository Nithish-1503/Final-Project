apiVersion: v1
kind: Service
metadata:
  name: mysql
  namespace: trip-planner
spec:
  selector:
    app: mysql
  ports:
    - port: 3306
      targetPort: 3306
  clusterIP: None   # headless service for the stateful DB
